# Урок 2. Домашняя работа — Переменные и типы

**Имя:** _________________________  **Дата:** ____________

Памятка:
```
пер Имя = "Аня"          // переменная (можно менять)
знч Пи = 3.14            // константа (менять нельзя)
пер Возраст: Число = 12  // с типом
```
Типы: `Строка`, `Число`, `Булево` (`Истина` / `Ложь`).

---

## Часть А. Разминка ⭐

**А1.** Создай переменную `Питомец` и выведи её.

**А2.** Создай переменную `Год = 2026` и выведи её.

**А3. Тип — текст или число?** Подпиши:
```
"кот"   → ____      42      → ____
"42"    → ____      3.5     → ____
Истина  → ____      "да"    → ____
```

**А4. Предскажи вывод:**
```
метод Скрипт()
    пер Х = 3
    Х = Х + 7
    Консоль.Записать(Х)   // ?
;
```

**А5.** Создай константу `знч МаксЗдоровье = 100` и выведи её.

---

## Часть Б. Основные задачи ⭐⭐

**Б1. Карточка героя.** Переменные `Имя`, `Сила` (число), `Жив` (булево).
Выведи все три.

**Б2. Прокачка.** Возьми героя из Б1. Увеличь `Сила` на 25, потом ещё
умножь на 2. Выводи `Сила` после каждого изменения.

**Б3. Счёт матча.** Заведи `ОчкиХозяев = 0` и `ОчкиГостей = 0`.
Поменяй счёт три раза (гол туда, гол сюда) и выводи его после каждого гола.

**Б4. Что в коробочках?** Предскажи каждую строку, потом проверь:
```
метод Скрипт()
    пер А = 10
    пер Б = А
    А = 99
    Консоль.Записать(А)   // ?
    Консоль.Записать(Б)   // ?
;
```

**Б5. Хорошие имена.** Перепиши с понятными именами:
```
пер п = "Москва"
пер т = 15
```

---

## Часть В. Со звёздочкой ⭐⭐⭐

**В1. Обмен.** `пер А = "день"`, `пер Б = "ночь"`. Поменяй их значения
местами через переменную-помощник. Проверь выводом.

**В2. Тройной сдвиг.** `А = 1`, `Б = 2`, `В = 3`. Сделай `А = 3`, `Б = 1`,
`В = 2`.

**В3. Кошелёк.** Заведи `Деньги = 500`. Соверши 3 «покупки» (вычитай суммы)
и выводи остаток после каждой.

---

## Что сдать

- [ ] Часть А полностью
- [ ] Б1–Б4
- [ ] хотя бы В1
