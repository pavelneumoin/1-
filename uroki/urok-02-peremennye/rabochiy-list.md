# Урок 2. Рабочий лист — Переменные и типы

**Имя:** _________________________  **Дата:** ____________

⭐ — обязательно · ⭐⭐ — основное · ⭐⭐⭐ — со звёздочкой.

---

## Памятка

```
пер Имя = "Аня"        // переменная (можно менять)
знч Пи = 3.14          // константа (менять нельзя)
пер Возраст: Число = 12   // с указанием типа
```

Типы: `Строка` (текст), `Число`, `Булево` (`Истина` / `Ложь`).

---

## Задание 1 ⭐ — Создай и выведи

Создай переменную `Город` со значением своего города и выведи её.

```
метод Скрипт()

;
```

---

## Задание 2 ⭐ — Три коробочки

Создай три переменные: `Имя` (строка), `Возраст` (число),
`ЛюбитИгры` (булево). Выведи все три.

```
метод Скрипт()



;
```

---

## Задание 3 ⭐⭐ — Меняем значение

Что выведет программа? Запиши ответ **рядом с каждой строкой**, не запуская.

```
метод Скрипт()
    пер Очки = 10
    Консоль.Записать(Очки)      // ?
    Очки = 25
    Консоль.Записать(Очки)      // ?
    Очки = Очки + 5
    Консоль.Записать(Очки)      // ?
;
```

Теперь запусти и проверь себя.

---

## Задание 4 ⭐⭐ — Строка или число?

Подпиши, что в каждой переменной — **текст** или **число**:

```
пер А = 7        →  ____________
пер Б = "7"      →  ____________
пер В = "семь"   →  ____________
пер Г = 3.14     →  ____________
```

---

## Задание 5 ⭐⭐ — Константа

Создай константу `знч ДнейВНеделе = 7` и выведи её. Затем попробуй
ниже написать `ДнейВНеделе = 8` и запусти. Что произошло?

Ответ: ___________________________________________________________

---

## Задание 6 ⭐⭐ — Хорошие имена

Перепиши программу, дав переменным понятные имена:

```
метод Скрипт()
    пер х = "Барсик"
    пер у = 3
    Консоль.Записать(х)
    Консоль.Записать(у)
;
```

Твой вариант:

```
метод Скрипт()



;
```

---

## Задание 7 ⭐⭐⭐ — Обмен значений

Есть две переменные:

```
пер А = "лево"
пер Б = "право"
```

Сделай так, чтобы после твоего кода `А` хранила `"право"`, а `Б` — `"лево"`.

Подсказка: понадобится третья переменная-«помощник».

```
метод Скрипт()
    пер А = "лево"
    пер Б = "право"
    // твой код



    Консоль.Записать(А)   // должно быть: право
    Консоль.Записать(Б)   // должно быть: лево
;
```

---

## Домашняя практика

**ДЗ-1 ⭐ — Карточка героя.** Переменные `Имя`, `Сила` (число),
`Жив` (булево). Выведи их. Затем увеличь `Сила` на 10 и снова выведи.

**ДЗ-2 ⭐⭐ — Пять фактов о себе.** Создай 5 переменных-фактов
(например, `Рост`, `ЛюбимыйПредмет`, `ЕстьПитомец` и т. д.) с понятными
именами и выведи их.

**ДЗ-3 ⭐⭐ — Счёт в игре.** Заведи переменную `Очки = 0`. Тремя
действиями доведи её до `100` (любым способом: прибавляя и умножая),
выводя значение после каждого действия.

**ДЗ-4 ⭐⭐⭐ — Тройной обмен.** Есть три переменные `А = 1`, `Б = 2`,
`В = 3`. Сделай так, чтобы стало `А = 3`, `Б = 1`, `В = 2`. Проверь выводом.
