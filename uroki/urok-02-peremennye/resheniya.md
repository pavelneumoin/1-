# Урок 2. Решения (для преподавателя)

Цель урока: ученик создаёт переменные (`пер`) и константы (`знч`),
различает типы `Строка` / `Число` / `Булево`, умеет менять значение.

---

## Задание 1 ⭐

```
метод Скрипт()
    пер Город = "Москва"
    Консоль.Записать(Город)
;
```

---

## Задание 2 ⭐

```
метод Скрипт()
    пер Имя: Строка = "Аня"
    пер Возраст: Число = 12
    пер ЛюбитИгры: Булево = Истина
    Консоль.Записать(Имя)
    Консоль.Записать(Возраст)
    Консоль.Записать(ЛюбитИгры)
;
```

Тип можно и не указывать — засчитываем оба варианта.

---

## Задание 3 ⭐⭐

Ожидаемый вывод:

```
10
25
30
```

Разбор последней строки: `Очки = Очки + 5` → было 25, стало 30.

---

## Задание 4 ⭐⭐

```
пер А = 7        →  число
пер Б = "7"      →  текст (строка)
пер В = "семь"   →  текст (строка)
пер Г = 3.14     →  число
```

Ключевая идея: кавычки делают значение текстом, без кавычек число — это число.

---

## Задание 5 ⭐⭐

```
метод Скрипт()
    знч ДнейВНеделе = 7
    Консоль.Записать(ДнейВНеделе)
    // ДнейВНеделе = 8   ← вызовет ошибку: знч менять нельзя
;
```

Ответ: среда выдаёт ошибку, потому что `знч` — константа, её значение
изменить нельзя.

---

## Задание 6 ⭐⭐

```
метод Скрипт()
    пер КличкаКота = "Барсик"
    пер ВозрастКота = 3
    Консоль.Записать(КличкаКота)
    Консоль.Записать(ВозрастКота)
;
```

Имена — любые осмысленные. Главное — стало понятнее, что хранится.

---

## Задание 7 ⭐⭐⭐ — Обмен

```
метод Скрипт()
    пер А = "лево"
    пер Б = "право"

    пер Помощник = А    // запомнили "лево"
    А = Б               // А стало "право"
    Б = Помощник        // Б стало "лево"

    Консоль.Записать(А)   // право
    Консоль.Записать(Б)   // лево
;
```

Классическая задача на обмен через временную переменную. Если ученик
попробует без помощника (`А = Б; Б = А`) — оба станут `"право"`; это
отличный повод разобрать, почему так.

---

## Домашнее задание — пример

```
метод Скрипт()
    пер Имя = "Рыцарь"
    пер Сила = 50
    пер Жив = Истина
    Консоль.Записать(Имя)
    Консоль.Записать(Сила)
    Консоль.Записать(Жив)

    Сила = Сила + 10
    Консоль.Записать(Сила)   // 60
;
```

---

## Типичные ошибки

- Выводят `"Имя"` в кавычках и удивляются, почему печатается слово, а не
  значение. Показать разницу `Записать(Имя)` и `Записать("Имя")`.
- Пробуют умножать `"5"` (строку). Связать с уроком про числа (урок 4).
- Путают `пер` и `знч` — напомнить: `знч` про «значение, которое не меняется».
