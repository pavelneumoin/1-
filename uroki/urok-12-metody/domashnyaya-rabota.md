# Урок 12. Домашняя работа — Методы

**Имя:** _________________________  **Дата:** ____________

Памятка:
```
метод Имя()
    Консоль.Записать("...")
;

метод Скрипт()
    Имя()
;
```

---

## Часть А. Разминка ⭐

**А1.** Создай метод `Салют()`, выводящий `Ура!`, и вызови его из `Скрипт`.

**А2.** Вызови `Салют()` четыре раза.

**А3.** Создай метод `Имя()`, выводящий твоё имя, и вызови дважды.

**А4. Сколько строк выведет программа?**
```
метод Эхо()
    Консоль.Записать("эй")
;
метод Скрипт()
    Эхо()
    Эхо()
    Эхо()
;
```
Ответ: ______

---

## Часть Б. Основные задачи ⭐⭐

**Б1. Рамка.** Метод `Рамка()` выводит строку из 15 символов `=`. Вызови
его до и после своего имени.

**Б2. Шапка меню.** Метод `Шапка()` выводит три строки: рамку, слово
`ИГРА`, рамку. Вызови дважды.

**Б3. Приветствие дня.** Метод `Утро()` выводит «Доброе утро!», метод
`Вечер()` — «Добрый вечер!». Вызови оба.

**Б4. Найди ошибку.** Объясни, почему ничего не выводится, и исправь:
```
метод Привет()
    Консоль.Записать("Привет")
;
метод Скрипт()
;
```

---

## Часть В. Со звёздочкой ⭐⭐⭐

**В1. Флаг.** Сделай три метода — `Белая()`, `Синяя()`, `Красная()`
(каждый выводит строку из своего «цвета», например `БББББ`). Собери из них
флаг в `Скрипт`.

**В2. Лесенка-узор.** Методы `А()` и `Б()` выводят разные строки. Вызывая
их в нужном порядке, собери узор из 6 строк.

**В3. Свой «логотип».** Придумай метод `Лого()`, выводящий мини-картинку
из символов (4–5 строк), и вызови его в начале и в конце программы.

---

## Что сдать

- [ ] Часть А полностью
- [ ] Б1–Б4
- [ ] хотя бы В1
