# Урок 12. Рабочий лист — Методы

**Имя:** _________________________  **Дата:** ____________

⭐ — обязательно · ⭐⭐ — основное · ⭐⭐⭐ — со звёздочкой.

---

## Памятка

```
метод Привет()
    Консоль.Записать("Привет!")
;

метод Скрипт()
    Привет()
;
```

Метод — своя команда. Объявил `метод Имя() ... ;`, вызвал `Имя()`.

---

## Задание 1 ⭐ — Первый метод

Создай метод `Привет()`, который выводит `Привет!`, и вызови его из `Скрипт`.

```
метод Привет()

;

метод Скрипт()

;
```

---

## Задание 2 ⭐ — Три вызова

Вызови свой метод `Привет()` три раза. Сколько строк появится?

Ответ: ______

---

## Задание 3 ⭐⭐ — Линия

Создай метод `Линия()`, который выводит строку из 20 чёрточек `-`.
Вызови его до и после строки с твоим именем.

```
метод Линия()

;

метод Скрипт()

;
```

---

## Задание 4 ⭐⭐ — Шапка

Создай метод `Шапка()`, который выводит три строки рамки с надписью `МЕНЮ`,
и вызови его дважды.

```
метод Шапка()



;

метод Скрипт()

;
```

---

## Задание 5 ⭐⭐ — Найди ошибку

Почему программа ничего не выводит? Исправь.

```
метод Привет()
    Консоль.Записать("Привет!")
;

метод Скрипт()
;
```

Причина: ____________________________________________

---

## Задание 6 ⭐⭐⭐ — Узор

Создай два метода: `Звезда()` (выводит `*****`) и `Точки()` (выводит `.....`).
Вызови их по очереди, чтобы получить узор из 4 строк:

```
*****
.....
*****
.....
```

```
метод Скрипт()



;
```

---

## Блиц-практика ⭐

1. Создай метод `Гудок()`, выводящий `Бип!`, вызови один раз.
2. Вызови `Гудок()` пять раз.
3. Создай метод `Пусто()`, который выводит пустую строку `" "`.
4. Создай метод, выводящий твоё имя, и вызови дважды.

---

## Домашняя работа

Домашние задания — в листе **`domashnyaya-rabota.md`** (части А, Б, В).
