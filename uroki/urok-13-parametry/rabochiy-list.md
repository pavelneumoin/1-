# Урок 13. Рабочий лист — Параметры и возврат

**Имя:** _________________________  **Дата:** ____________

⭐ — обязательно · ⭐⭐ — основное · ⭐⭐⭐ — со звёздочкой.

---

## Памятка

```
метод Поздороваться(Имя: Строка)       // принимает данные
    Консоль.Записать("Привет, $Имя!")
;

метод Сумма(А: Число, Б: Число): Число  // возвращает ответ
    возврат А + Б
;
```

---

## Задание 1 ⭐ — Привет по имени

Создай метод `Привет(Имя: Строка)`, который выводит `Привет, <имя>!`.
Вызови его с двумя разными именами.

```
метод Привет(Имя: Строка)

;

метод Скрипт()


;
```

---

## Задание 2 ⭐ — Сумма

Создай метод `Сумма(А: Число, Б: Число): Число`, возвращающий сумму.
Выведи `Сумма(4, 6)`.

```
метод Сумма(А: Число, Б: Число): Число

;

метод Скрипт()

;
```

---

## Задание 3 ⭐⭐ — Площадь

Метод `Площадь(Ш: Число, В: Число): Число` возвращает площадь.
Выведи площади двух разных прямоугольников.

```
метод Площадь(Ш: Число, В: Число): Число

;

метод Скрипт()


;
```

---

## Задание 4 ⭐⭐ — Удвоить

Метод `Удвоить(N: Число): Число` возвращает число, умноженное на 2.
Что выведет программа?

```
метод Удвоить(N: Число): Число
    возврат N * 2
;
метод Скрипт()
    пер X = Удвоить(5)
    пер Y = Удвоить(X)
    Консоль.Записать(Y)      // ?
;
```

Ответ: ______

---

## Задание 5 ⭐⭐ — Максимум

Создай метод `Максимум(А: Число, Б: Число): Число`, возвращающий большее
из двух чисел. Проверь на двух парах.

```
метод Максимум(А: Число, Б: Число): Число

;

метод Скрипт()


;
```

---

## Задание 6 ⭐⭐⭐ — Сумма площадей

Используя метод `Площадь` из задания 3, посчитай в одной строке сумму
площадей прямоугольников 3×4 и 5×2.

```
метод Скрипт()

;
```

---

## Блиц-практика ⭐

1. Метод `Куб(N): Число` возвращает `N * N * N`. Выведи `Куб(3)`.
2. Метод `Разность(А, Б): Число`. Выведи `Разность(10, 4)`.
3. Метод `Привет(Имя)` — поздоровайся с тремя именами.
4. Метод `Среднее(А, Б): Число` возвращает `(А + Б) / 2`. Выведи `Среднее(4, 8)`.

---

## Домашняя работа

Домашние задания — в листе **`domashnyaya-rabota.md`** (части А, Б, В).
