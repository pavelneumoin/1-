# Урок 14. Домашняя работа — Разбиваем на методы

**Имя:** _________________________  **Дата:** ____________

Памятка: один метод — одно дело; методы вызывают другие методы.

---

## Часть А. Разминка ⭐

**А1.** Метод `Квадрат(N): Число`. Выведи `Квадрат(7)`.

**А2.** Метод `Двойное(N): Число`. Выведи `Двойное(9)`.

**А3.** Используя `Двойное`, выведи `Двойное(Двойное(3))`.

**А4. Что вернёт `Ф(3)`?**
```
метод Г(N: Число): Число
    возврат N + 2
;
метод Ф(N: Число): Число
    возврат Г(N) * Г(N)
;
```
Ответ: ______

---

## Часть Б. Основные задачи ⭐⭐

**Б1. Сумма квадратов.** Методы `Квадрат(n)` и `СуммаКвадратов(а, б)`.
Выведи `СуммаКвадратов(3, 4)`.

**Б2. Прямоугольник.** Методы `Площадь(а, б)` и `Периметр(а, б)`. Спроси
стороны, выведи оба результата.

**Б3. Среднее из суммы.** Метод `Сумма(а, б)` и метод `Среднее(а, б)`,
который использует `Сумма`. Выведи среднее двух чисел.

**Б4. Хорошие имена.** Дай понятные имена методам:
```
метод А(ц: Число, к: Число): Число
    возврат ц * к
;
```

---

## Часть В. Со звёздочкой ⭐⭐⭐

**В1. Цена покраски.** Методы `Площадь(а, б)` и `ЦенаПокраски(s)` (s × 50).
Посчитай цену покраски стены 4×5.

**В2. Стоимость заказа.** Методы `Стоимость(цена, кол)` и `СоСкидкой(сумма)`
(минус 10%). Посчитай итог заказа: 3 товара по 200 со скидкой.

**В3. Свой «конструктор».** Придумай 2–3 маленьких метода и собери из них
один большой, решающий полезную задачу (например, расчёт похода в кино на
компанию). Опиши словами, что делает каждый метод.

---

## Что сдать

- [ ] Часть А полностью
- [ ] Б1–Б4
- [ ] хотя бы В1 и В2
