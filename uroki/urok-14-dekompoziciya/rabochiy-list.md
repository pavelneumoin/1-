# Урок 14. Рабочий лист — Разбиваем на методы

**Имя:** _________________________  **Дата:** ____________

⭐ — обязательно · ⭐⭐ — основное · ⭐⭐⭐ — со звёздочкой.

---

## Памятка

```
метод Квадрат(N: Число): Число
    возврат N * N
;
метод СуммаКвадратов(А: Число, Б: Число): Число
    возврат Квадрат(А) + Квадрат(Б)
;
```

Один метод — одно дело. Методы можно вызывать из других методов.

---

## Задание 1 ⭐ — Квадрат

Создай метод `Квадрат(N): Число`, возвращающий `N * N`. Выведи `Квадрат(6)`.

```
метод Квадрат(N: Число): Число

;
метод Скрипт()

;
```

---

## Задание 2 ⭐⭐ — Сумма квадратов

Используя `Квадрат` из задания 1, сделай метод
`СуммаКвадратов(А, Б): Число` и выведи `СуммаКвадратов(3, 4)`.

```
метод СуммаКвадратов(А: Число, Б: Число): Число

;
```

Ответ должен быть: ______

---

## Задание 3 ⭐⭐ — Площадь и периметр

Раздели задачу на два метода — `Площадь(а, б)` и `Периметр(а, б)`.
В `Скрипт` спроси стороны и выведи оба результата через методы.

```
метод Площадь(А: Число, Б: Число): Число

;
метод Периметр(А: Число, Б: Число): Число

;
метод Скрипт()



;
```

---

## Задание 4 ⭐⭐ — Что вернёт `Финал(2)`?

```
метод Плюс1(N: Число): Число
    возврат N + 1
;
метод Финал(N: Число): Число
    возврат Плюс1(N) * Плюс1(N)
;
```

Ответ: ______

---

## Задание 5 ⭐⭐⭐ — Цена покраски

Сделай методы `Площадь(а, б)` и `ЦенаПокраски(площадь): Число`
(площадь × 50). В `Скрипт` посчитай цену покраски стены 3×4 одной строкой.

```
метод Скрипт()

;
```

---

## Задание 6 ⭐⭐⭐ — Хорошие имена

Дан метод с плохим именем. Переименуй его так, чтобы было понятно, что он
делает, не читая код:

```
метод Х(а: Число, б: Число): Число
    возврат (а + б) / 2
;
```

Новое имя: ____________________

---

## Блиц-практика ⭐

1. Метод `Куб(N): Число`. Выведи `Куб(2)`.
2. Метод `Двойное(N): Число` возвращает `N * 2`. Выведи `Двойное(Куб(2))`.
3. Метод `Привет()` + метод `Пока()`. Вызови оба.
4. Метод `Среднее(а, б)` собери из `Сумма(а, б)`: `Сумма(а, б) / 2`.

---

## Домашняя работа

Домашние задания — в листе **`domashnyaya-rabota.md`** (части А, Б, В).
