# Урок 15. Рабочий лист — Когда что-то идёт не так

**Имя:** _________________________  **Дата:** ____________

⭐ — обязательно · ⭐⭐ — основное · ⭐⭐⭐ — со звёздочкой.

---

## Памятка

```
если Б = 0 тогда
    Консоль.Записать("На ноль делить нельзя!")
иначе
    Консоль.Записать("Результат: " + (А / Б))
;
```

Проверяй опасные данные **до** действия. Главный инструмент — `если`.

---

## Задание 1 ⭐ — Не отрицательный

Спроси возраст. Если он меньше 0 — выведи `Так не бывает`, иначе — `Ок`.

```
метод Скрипт()

;
```

---

## Задание 2 ⭐ — Деление с защитой

Спроси два числа и раздели первое на второе. Если второе равно 0 —
сообщи об этом, иначе выведи результат.

```
метод Скрипт()

;
```

---

## Задание 3 ⭐⭐ — Оценка в рамках

Спроси оценку. Если она от 2 до 5 — выведи `Принято`, иначе —
`Нужно от 2 до 5`.

```
метод Скрипт()

;
```

---

## Задание 4 ⭐⭐ — Метод-страж

Создай метод `Корректный(Оценка: Число): Булево`, который возвращает
`Истина`, если оценка от 2 до 5. Используй его в `если`.

```
метод Корректный(Оценка: Число): Булево

;
метод Скрипт()


;
```

---

## Задание 5 ⭐⭐ — Что выведет при вводе `0`?

```
метод Скрипт()
    пер N = Консоль.СчитатьЧисло("Число: ")
    если N > 0 тогда
        Консоль.Записать("Плюс")
    иначеесли N < 0 тогда
        Консоль.Записать("Минус")
    иначе
        Консоль.Записать("Ноль")
    ;
;
```

Ответ: ______

---

## Задание 6 ⭐⭐⭐ — Безопасный калькулятор

Сделай метод `Делить(А: Число, Б: Число)` с защитой от деления на ноль.
Проверь на `10 / 2` и `10 / 0`.

```
метод Делить(А: Число, Б: Число)

;
метод Скрипт()


;
```

---

## Блиц-практика ⭐

1. Спроси число. Если больше 100 — `Много`, иначе — `Нормально`.
2. Спроси число. Выведи `Чётное` или `Нечётное` (защити от... да просто
   проверь остаток).
3. Спроси возраст. Если от 0 до 120 — `Ок`, иначе — `Проверь ввод`.

---

## Домашняя работа

Домашние задания — в листе **`domashnyaya-rabota.md`** (части А, Б, В).
