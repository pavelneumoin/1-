# Урок 16. Домашняя работа — Проект и повторение модуля 3

**Имя:** _________________________  **Дата:** ____________

Всё — на методах. Каждая программа отдельная и должна запускаться.

---

## Часть А. Доделать помощника ⭐

**А1.** Доведи «Карманный помощник» до полной версии: три метода-расчётчика,
меню, выбор действия, ветка «неизвестное действие».

**А2.** Добавь к выводу подпись, какое действие выполнено
(например, `Действие: площадь`).

---

## Часть Б. Новые методы ⭐⭐

**Б1. Добавь действия.** Допиши методы `Сумма(а, б)` и `Разность(а, б)` и
два новых пункта меню.

**Б2. Метод-страж.** `Корректный(Выбор): Булево` → `Истина`, если выбор от
1 до 5. Используй его перед расчётом.

**Б3. Безопасное деление.** Добавь действие «деление» с защитой от нуля
(отдельным методом `Делить`).

**Б4. Калькулятор-функции.** Сделай отдельную программу из методов
`Сложить`, `Вычесть`, `Умножить` и выведи все три результата для пары чисел.

---

## Часть В. Большой проект ⭐⭐⭐

Выбери **одну** идею и собери её из методов:

**В1. Помощник с циклом.** Меню повторяется, пока не введут `0`. Каждый
расчёт — свой метод.

**В2. Конвертер величин.** Методы `КмВМетры`, `ЧасыВМинуты`, `РублиВКопейки`
(каждый возвращает число). Меню выбора, что перевести.

**В3. Школьный калькулятор.** Методы `Среднее` (три оценки), `Округлить`
(если доступно) или `ИтогСловами(средний)` — возвращает строку оценки
(`5 → Отлично` и т. д. через `если`).

---

## Что сдать

- [ ] Часть А (рабочий помощник)
- [ ] любые 2 задачи из части Б
- [ ] один проект из части В

Будь готов(а) показать и объяснить, из каких методов собрана программа.
